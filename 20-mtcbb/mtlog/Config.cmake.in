@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# 查找依赖的 Boost 组件
find_dependency(Boost REQUIRED COMPONENTS filesystem thread log log_setup)

# 包含导出的目标
include("${CMAKE_CURRENT_LIST_DIR}/mtlogTargets.cmake")

check_required_components(mtlog)
